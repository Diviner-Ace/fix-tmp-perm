# fix_tmp_daemon 部署说明

## 项目简介

`fix_tmp_daemon` 是一个运行在 Android 后台的 Rust 守护进程，用于解决以下问题：

- 虚拟定位应用 `com.suseoaa.locationspoofer` 启动时会将 `/data/local/tmp` 的权限强制改为 `755`
- 该应用退出后不会恢复权限，导致环境检测无法通过
- 本守护进程会监控该应用的运行状态，在其完全退出后自动将 `/data/local/tmp` 恢复为 `771`（所有者 `shell:shell`）

---

## 一、编译方式

### 方式 A：GitHub Actions 云端自动编译（推荐，零环境配置）

1. 将本项目推送到 GitHub 仓库
2. GitHub Actions 会自动触发编译（`.github/workflows/build.yml`）
3. 编译完成后，在仓库页面点击 **Actions** → 最新的 **Build Android ARM64 Binary** 工作流
4. 页面底部 **Artifacts** 区域下载 `fix_tmp_daemon-android-arm64`
5. 解压后得到二进制文件 `fix_tmp_daemon`

### 方式 B：本地手动编译

#### 前置条件

| 工具 | 安装命令 / 说明 |
|------|----------------|
| Rust | `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \| sh` |
| Android NDK | 下载 r26c 或更高版本，设置 `ANDROID_NDK_HOME` 环境变量 |
| cargo-ndk | `cargo install cargo-ndk` |
| Android 目标 | `rustup target add aarch64-linux-android` |

#### 编译命令

```bash
cd fix_tmp_daemon
cargo ndk -t arm64-v8a --platform 30 build --release
```

编译产物路径：
```
target/aarch64-linux-android/release/fix_tmp_daemon
```

---

## 二、部署到手机

### 前置条件

- 手机已 Root（KernelSU / Magisk 均可）
- 已安装支持 Root 的文件管理器（推荐 **MT 管理器**）

### 部署步骤

#### 第 1 步：传输文件到手机

将以下两个文件传到手机任意位置（如 `Download` 目录）：

| 文件 | 说明 |
|------|------|
| `fix_tmp_daemon` | 编译好的二进制文件 |
| `99_start_rust.sh` | 开机自启脚本 |

#### 第 2 步：移动到 service.d 目录

使用 MT 管理器（已授予 Root 权限）：

1. 将 `fix_tmp_daemon` 移动到 `/data/adb/service.d/`
2. 将 `99_start_rust.sh` 移动到 `/data/adb/service.d/`

> `/data/adb/service.d/` 是 KernelSU / Magisk 的开机脚本目录，放在这里的脚本会在开机时自动以 root 权限执行。

#### 第 3 步：设置权限

在 MT 管理器中，对每个文件**长按 → 属性 → 权限**：

**fix_tmp_daemon：**
```
所有者: root (0)
用户组: root (0)
权限:   rwxr-xr-x (755)
```

**99_start_rust.sh：**
```
所有者: root (0)
用户组: root (0)
权限:   rwxr-xr-x (755)
```

> 也可以通过 ADB 命令设置：
> ```bash
> adb shell su -c "chmod 755 /data/adb/service.d/fix_tmp_daemon"
> adb shell su -c "chmod 755 /data/adb/service.d/99_start_rust.sh"
> adb shell su -c "chown root:root /data/adb/service.d/fix_tmp_daemon"
> adb shell su -c "chown root:root /data/adb/service.d/99_start_rust.sh"
> ```

#### 第 4 步：确认换行符（重要！）

`99_start_rust.sh` 必须使用 **LF（Unix）** 换行符，不能使用 **CRLF（Windows）**。

- MT 管理器中打开脚本，如果每行末尾没有 `^M` 符号即为正确
- 如果是 CRLF，在 MT 管理器中点击右上角 → 转换换行符 → LF

> 本项目提供的脚本已确保为 LF 换行，正常情况下无需转换。

#### 第 5 步：重启手机

```bash
adb reboot
```

或直接在手机上重启。

---

## 三、验证部署是否成功

### 1. 检查守护进程是否在运行

```bash
adb shell su -c "ps -A | grep fix_tmp_daemon"
```

预期输出（能看到进程即成功）：
```
root          1234     1  ...  fix_tmp_daemon
```

### 2. 查看运行日志

```bash
adb shell su -c "cat /data/adb/service.d/fix_tmp_daemon.log"
```

预期日志内容：
```
[1726xxxxxx] ===== fix_tmp_daemon 启动 =====
[1726xxxxxx] 监控目标: com.suseoaa.locationspoofer
[1726xxxxxx] 目标目录: /data/local/tmp
[1726xxxxxx] 系统开机完成，等待稳定...
[1726xxxxxx] 系统已稳定，开始监控循环
[1726xxxxxx] 启动时权限修复成功
```

### 3. 功能测试

1. 确保 `com.suseoaa.locationspoofer` **未运行**
2. 检查当前权限：
   ```bash
   adb shell su -c "ls -ld /data/local/tmp"
   ```
   预期输出：
   ```
   drwxrwx--x 2 shell shell ... /data/local/tmp
   ```
3. 启动 `com.suseoaa.locationspoofer`，等待几秒后完全退出
4. 等待约 8 秒（3 秒确认 + 5 秒巡检周期）
5. 再次检查权限，应已恢复为 `drwxrwx--x shell shell`

---

## 四、工作原理

```
开机
 │
 ├─► KernelSU 执行 /data/adb/service.d/99_start_rust.sh
 │       │
 │       └─► 启动 fix_tmp_daemon（后台 &）
 │               │
 │               ├─► daemonize（double-fork + setsid，完全脱离终端）
 │               ├─► 等待 sys.boot_completed == 1
 │               ├─► 等待 10 秒系统稳定
 │               ├─► 启动时若应用未运行，先修复一次权限
 │               │
 │               └─► 无限循环（每 5 秒）
 │                       │
 │                       ├─► 读取 /proc/*/cmdline 判断应用是否运行
 │                       │
 │                       ├─► 应用运行中 → 不干扰，继续监控
 │                       │
 │                       └─► 应用从运行→退出
 │                               │
 │                               ├─► 等待 3 秒确认无残留进程
 │                               ├─► libc::chmod(0o771)
 │                               ├─► libc::chown(2000, 2000)
 │                               └─► 验证权限并记录日志
```

---

## 五、功耗说明

| 阶段 | CPU 占用 | 说明 |
|------|---------|------|
| 等待开机 | 0% | `thread::sleep` 内核级挂起 |
| 巡检间隔（5 秒） | 0% | 线程完全挂起，不消耗 CPU |
| 巡检瞬间 | < 1% | 读取 `/proc` 目录，毫秒级完成 |
| 权限修复 | < 1% | 仅在应用退出时执行一次 |

- 全程仅 **1 个常驻进程**，无 fork 子进程
- `thread::sleep` 直接调用内核 `nanosleep`，不拉起任何外部进程
- 读取 `/proc` 为纯内存操作，无磁盘 IO
- release 编译开启 LTO + strip，二进制体积通常 **< 300KB**

---

## 六、常见问题

### Q1: 重启后进程没有运行？

**排查步骤：**
1. 检查脚本权限是否为 755：`ls -l /data/adb/service.d/`
2. 检查脚本换行符是否为 LF
3. 手动执行脚本看是否报错：
   ```bash
   adb shell su -c "sh /data/adb/service.d/99_start_rust.sh"
   ```
4. 检查二进制是否为 ARM64 架构：
   ```bash
   adb shell su -c "file /data/adb/service.d/fix_tmp_daemon"
   ```

### Q2: 权限没有被恢复？

1. 查看日志：`cat /data/adb/service.d/fix_tmp_daemon.log`
2. 确认包名是否正确：`pm list packages | grep suseoaa`
3. 确认应用是否真的完全退出（有些应用有后台服务）

### Q3: 如何卸载？

```bash
adb shell su -c "rm /data/adb/service.d/fix_tmp_daemon"
adb shell su -c "rm /data/adb/service.d/99_start_rust.sh"
adb shell su -c "rm /data/adb/service.d/fix_tmp_daemon.log"
adb reboot
```

### Q4: 如何修改巡检间隔或目标包名？

编辑 `src/main.rs` 顶部的常量：
```rust
const TARGET_PKG: &str = "com.suseoaa.locationspoofer";  // 目标包名
const POLL_INTERVAL: u64 = 5;                              // 巡检间隔（秒）
```
修改后重新编译并替换二进制文件即可。

---

## 七、文件清单

| 文件 | 路径 | 说明 |
|------|------|------|
| `Cargo.toml` | 项目根目录 | Rust 项目配置 + release 体积优化 |
| `src/main.rs` | 项目根目录 | 守护进程完整源码 |
| `99_start_rust.sh` | 项目根目录 | KernelSU 开机自启脚本 |
| `.github/workflows/build.yml` | 项目根目录 | GitHub Actions 自动编译工作流 |
| `部署说明.md` | 项目根目录 | 本文档 |
